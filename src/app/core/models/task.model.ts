export interface TaskModel {

    /*  Un título (string)
    *   Una descripción (string)
    *   Un estado: "pendiente" o "completada" (string)
    */

    title: string;
    description: string;
    status: string;
}